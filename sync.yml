trigger:
  branches:
    include:
    - main

resources:
  repositories:
    - repository: templates
      type: git
      name: Plumbing/core-templates
    - repository: observe.js
      type: git
      name: Open/observe.js

stages:
- template: stages/sync/sync.yml@templates  # Template reference
  parameters:
    checkout: observe.js
